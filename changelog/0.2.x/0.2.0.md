---
summary: "Canvas IDs are validated when supplied, OpenFEMA calls use one bounded exchange budget without exposing request URLs, sessions default to stateless with explicit overrides, and project tooling adopts the current framework conventions."
breaking: true
security: true
agent-notes: |
  With MCP_SESSION_MODE unset, the server defaults to stateless; an explicit MCP_SESSION_MODE still overrides it.

  When supplied, canvas_id must be a server-issued 10-character URL-safe ID. Read canvas_id from fema_search_nfip before calling the dataframe tools.

  The @cyanheads/mcp-ts-core upgrade accepts documented camelCase argument aliases and reports argument validation failures before handlers run.
---

# 0.2.0 — 2026-09-19

## Changed

- **Canvas tool inputs** — when supplied, `canvas_id` for `fema_search_nfip`, `fema_dataframe_describe`, `fema_dataframe_query`, and `fema_dataframe_drop` must match the server-issued `^[A-Za-z0-9_-]{10}$` format; invalid IDs are rejected as invalid arguments before canvas lookup.
- **Session mode** — With MCP_SESSION_MODE unset, the server defaults to stateless; an explicit MCP_SESSION_MODE still overrides it.
- **OpenFEMA request budget** — `FEMA_REQUEST_TIMEOUT_MS` now bounds one upstream exchange, including response-body reads, retries, and backoff.
- **Framework behavior** — `@cyanheads/mcp-ts-core` accepts documented camelCase argument aliases, reports invalid arguments before handlers run, and carries declared recovery hints in client error responses.
- **Project tooling and metadata** — framework guidance moved to `framework-skills/`, release PR automation and the visible CodeQL workflow were added, and the supported Bun version is now 1.4 or later.

## Fixed

- **`fema_dataframe_query`** — DataCanvas conversion failures report `sql_execution_error` with TRY_CAST recovery guidance.

## Security

- **OpenFEMA client errors** — error data no longer includes the full request URL, which can contain caller-provided query values.

## Dependencies

- `@cyanheads/mcp-ts-core` ^0.12.3 → ^0.13.6
- `@duckdb/node-api` ^1.5.5-r.4 → ^1.5.5-r.5, `zod` ^4.4.3 → ^4.6.5
- `@biomejs/biome` ^2.5.9 → ^2.5.14, `@types/node` ^26.2.0 → ^26.6.1, `ignore` ^7.0.6 → ^7.0.9, `tsc-alias` ^1.9.2 → ^1.9.5
- `vitest` ^4.1.11 → ^5.0.1
